# Backend dependencies for the Hybrid Routing Search feature.
# Pinned versions for reproducible builds.

# Web framework and ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Validation / settings
pydantic==2.10.4
pydantic-settings==2.7.1

# ORM (async) and PostGIS integration
SQLAlchemy[asyncio]==2.0.36
GeoAlchemy2==0.16.0
asyncpg==0.30.0

# Async HTTP client for the Time Estimation Service
httpx==0.28.1

# Used only by scripts/generate_route_cache.py (offline booth demo route
# pre-computation) -- not imported by the running FastAPI app.
requests==2.32.3
python-dotenv==1.0.1
